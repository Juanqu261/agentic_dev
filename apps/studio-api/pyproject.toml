[project]
name = "studio-api"
version = "0.1.0"
description = "FastAPI app wiring pod-brain and serving the AG-UI SSE endpoint"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.30.0",
    "pod-brain",
]

[tool.uv.sources]
pod-brain = { workspace = true }
